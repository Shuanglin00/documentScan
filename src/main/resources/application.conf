# DocumentScan Application Configuration
# Environment variables override these defaults

app {
  name = "DocumentScan"
  version = "1.0.0"
  environment = ${?APP_ENV}
  environment = "development"
}

mongodb {
  uri = ${?MONGODB_URI}
  uri = "mongodb://localhost:27017/?authSource=admin"
  database = "localhost"
  auto-index-creation = false
}

milvus {
  host = ${?MILVUS_HOST}
  host = "http://localhost"
  port = ${?MILVUS_PORT}
  port = 19530
  db-name = "milvus_local"
  collection-name = "embeddings_local"
  enable = true
}

redis {
  host = ${?REDIS_HOST}
  # 通过 wslwork 网关访问宿主机上的 Redis
  host = "localhost"
  port = ${?REDIS_PORT}
  port = 6380
  password = ${?REDIS_PASSWORD}
  database = 0
  timeout = 2000
}

ai {
  ollama {
    base-url = ${?OLLAMA_BASE_URL}
    # 通过 wslwork 网关访问宿主机上的 Ollama
    base-url = "http://172.19.0.1:11434"
    api-key = ${?OLLAMA_API_KEY}
    api-key = ""
    chat-model = ${?OLLAMA_CHAT_MODEL}
    chat-model = "qwen3:8b"
    embedding-model = ${?OLLAMA_EMBEDDING_MODEL}
    embedding-model = "bge-m3"
    timeout = 60
  }
}

server {
  port = ${?SERVER_PORT}
  port = 8080
  host = ${?SERVER_HOST}
  host = "0.0.0.0"
}

logging {
  level = ${?LOG_LEVEL}
  level = "INFO"
}
